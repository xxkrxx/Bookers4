<div class="search_form">
  <table align="center">
    <tr>
      <div class="serch_form">
        <%= form_with url: tagesearches_search_path,method: :get, local: true do |f| %>
        <td><%= text_field :content %></td>
        <td><%= f.submit'タグ検索' %></td>
        <% end 
      </div>
    </tr>
  </table>
</div>